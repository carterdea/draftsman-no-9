# AGENTS.md

This repository uses `CLAUDE.md` as the canonical agent instruction file.

If you are an agent that reads `AGENTS.md`, load and follow `CLAUDE.md` fully.

No additional agent-specific overrides are defined here.
